import styled from 'styled-components';
import tw from 'twin.macro';

export const Button = styled.button`
  ${tw`flex justify-center items-center rounded-lg border p-1 m-1 
	hover:ring-1 ring-primary-color`}
`;
